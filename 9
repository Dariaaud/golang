package main

import (
	"fmt"
)

func sum(c []int) int {
	ans := 0
	for i := 0; i < len(c); i += 1 {
		ans += c[i]
	}
	return ans
}

func main() {
	//9 задача
	arr := []int{}
	fmt.Scan(arr)
	fmt.Println(sum(arr))
}
